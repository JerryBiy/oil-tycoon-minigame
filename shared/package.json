{
  "name": "@oiltycoon/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types, network protocol, and config schemas used by both the Cocos client and the Node server.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
